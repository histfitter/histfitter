1. Execute:
root -b -q pvalue.C

to determine the CLs p-values (observed, expected, +/-1 sigma uncertainties) of the "simple channel" 
counting experiment created at :
analysis/simplechannel/README

The p-values are printed on the screen.
Options (e.g. exclusion or discovery) can be set in the macro.



